version: 0.2

phases: 
  pre-build: 
    commands:
      - cd my-app
      - npm install
    
  build:
    commands: 
      - npm run build 
      - aws s3 sync "./build" "s3://react-testbucket"

artifacts: 
  files:
  - "**/*"
  discard-paths: no
  base-directory: build